<UserControl x:Class="LocationFromGoogle.MVVM.View.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LocationFromGoogle.MVVM.View"
             xmlns:vm="clr-namespace:LocationFromGoogle.MVVM.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <!--<UserControl.DataContext>
        <vm:TermineVM/>
    </UserControl.DataContext>-->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <GroupBox Header="Termine" FontSize="14">
            <DataGrid x:Name="LBX_Termine" Grid.Row="1" ItemsSource="{Binding TerminListe}"/>
        </GroupBox>
    </Grid>
</UserControl>
